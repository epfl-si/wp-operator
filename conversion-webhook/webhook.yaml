---

apiVersion: v1
kind: Service
metadata:
  name: wordpresssite-conversion-webhook
  namespace: wordpress-test
spec:
    type: ClusterIP
    selector:
      app: wordpresssite-conversion-webhook
    ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 6443
